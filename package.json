{
  "name": "gemini-coder-toolkit",
  "version": "0.7.0-alpha",
  "description": "A collection of task-automation skills explicitly formatted and packaged for compatibility with the Gemini CLI environment.",
  "license": "GPL-3.0",
  "author": "Gemini CLI Users",
  "homepage": "https://github.com/google/gemini-coder-toolkit",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/google/gemini-coder-toolkit.git"
  },
  "keywords": [
    "gemini",
    "gemini-cli",
    "skill",
    "agent"
  ],
  "engines": {
    "gemini": ">=0.37.0"
  },
  "scripts": {
    "bundle": "brew bundle --file Brewfile.$RAILS_ENV",
    "install:extensions": "gemini extensions install https://github.com/gemini-cli-extensions/ralph --auto-update --consent",
    "test": "node scripts/validate_skills.test.cjs",
    "build": "rm -rf dist && mkdir -p dist && for d in skills/*; do if [ -d \"$d\" ]; then skill_name=$(basename \"$d\"); echo \"Packaging ${skill_name}...\"; (cd \"$d\" && zip -rq \"../../dist/${skill_name}.skill\" .); fi; done"
  },
  "packageManager": "yarn@4.17.0"
}
