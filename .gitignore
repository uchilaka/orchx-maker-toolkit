# Testing
coverage/

# Production
# We include dist/ because this repo is the distribution point
# dist/

# Misc
.DS_Store
*.log
.gemini/tmp/
.gemini/logs/

# `mise run mount:claude` symlinks .gemini/skills/* into .claude/skills/ so they're
# discoverable as Claude Code skills too. Ignore everything there except the
# repo's real, Claude-native skills, so a mount never gets committed as a duplicate.
.claude/skills/*
!.claude/skills/patch-my-hosts
!.claude/skills/llm-wikify

# Local configuration files
.env
.env.local
.env.*.local
