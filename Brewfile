cask_args appdir: '~/Applications', require_sha: true

tap 'protonpass/tap'

brew 'gh'
brew 'goreman'
brew 'gnupg'
brew 'git-crypt'
brew 'gitleaks'
brew 'gemini-cli'

cask 'antigravity-cli' if ENV['RAILS_ENV'] == 'development'
